\documentclass[a4paper,11pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage{graphicx}
\usepackage{color}
\usepackage{pdfpages}
\usepackage{pdflscape}

\begin{document}
\begin{titlepage}
    \noindent
    %\includegraphics[height=3cm]{TP2/univ.png}
    \hfill
    %\includegraphics[height=2.5cm]{TP2/ic2.png}
   \begin{center}
        \vspace{0.7cm}
        \huge \textbf{\textcolor{blue}{Le Mans Université}} \\ Licence Informatique \textit{2ème année} \\ Module 174UPO2 Rapport de Projet
    \end{center}
    \begin{center}
        \Huge \textbf{MAZE-SHOOTER} \\
        \vspace{0.7cm}
        \Large DEQUIREZ Thomas \\ JAMMES Corentin \\ ASTIER Hugues\\
        \vspace{0.7cm}
        \large \today
    \end{center}

\end{titlepage}



\tableofcontents 

\newpage
\section{Introduction}
\section{Conception}

\subsection{Cahier des charges}
Le cahier des charges à respecter suit les points suivants : 
\begin{itemize}
    \item Réaliser un jeu par groupe (de trois ici)
    \item Utiliser le language C
    \item Utiliser une librairie graphique comme SDL ou RAYLIB
    \item Se servir de GIT 
    \item Suivre un model de gestion de projet 
    \item Utiliser doxygen
\end{itemize} 

\smallskip
On a utilisé la librairie graphique Raylib, ainsi que GIT avec quatre branche (master, corentin, Thomas et HUgues) et on a suivit modèle linéaire.


\subsection{Fonctionnalités du jeu}
Dans MAZE-SHOOTER il y a deux modes de jeu principaux. Dans un premier temps, un mode solo, dans lequel le joueur doit survivre le plus longtemps 
possible face à des robots et des boss. Il doit en éliminer autant que possible.
 Et un mode multijoueur, dans lequel les joueurs s'affrontent entre eux dans un labyrinthe revisité pour facilité les combats longue distance.
Il est aussi possible de changer les textures via le menu "options".

\section{Organisation du projet}
\subsection{Reparition des tâches}
Dans le projet, les tâches ont été réparties dès le début et de façon assez claire. Thomas s’est occupé de la base conceptuelle du jeu. Il a créé le fichier main.c, et les initialisations du jeu : initialiser la caméra 3D, les bases des différentes entitées, le labyrinthe… Il a aussi conçu les déplacements des joueurs. C’est lui qui s’est occupé de gérer les principaux paramètres de Raylib, la bibliothèque graphique. Il a aussi conçu le système de logs, permettant un débogage simplifié. Thomas a aussi gérer les thèmes du jeu, permettant d’avoir différents ensemble de textures de sol et mur. Il a également implémenté le HUD (heads-up display) permettant aux joueurs de visualiser ses statistiques.

Corentin s’est occupé de la sauvegarde du jeu. Il a conçu le programme permettant de chiffrer l’état actuel du jeu dans un fichier avec clé de chiffrement, pour ne pas que l’on puisse modifier son état actuel. Il a également créé l’IA qui permet aux bots de chercher le joueur et de tirer. Le système de tir et de collision vient également de lui. Sa plus grosse contribution reste le système “multijoueur” avec le réseau. Il s’est occupé d’établir la connexion et le partage de données entre 2 machines, permettant à deux personnes de jouer simultanément sans problème de latence ou de bug.

Hugues s’est majoritairement occupé de la partie visuel du projet. Il a implémenté les modèles d’armes et ceux des bots ainsi que du coffre(soin et armes) et du joueur. Il a développé le système d’armes, permettant d’en débloquer au fur et à mesure dans des caisses ou en marquant plus de points. Il a par ailleurs conçu la physique des munitions, notamment celle de la grenade avec le rebond. Il a aussi fait l’HUD permettant d’afficher les armes débloquées par le joueur.


\subsection{Planning prévisionnel}



\subsection{Outils utilisés}
Pour ce projet, nous avons utilisé plusieurs outils pour faciliter le développement et la collaboration. Nous avons utilisé Git pour le contrôle de version,
ce qui nous a permis de travailler simultanément sur différentes parties du code sans conflits majeurs. Pour la gestion des tâches et du planning,  nous avons
utilisé un diagramme de Gantt, où nous avons assigné des tâches à chaque membre de l'équipe. Pour la gestion du rendu graphique, nous avons utilisé la 
librairie raylib, qui nous a permis de créer des graphismes 2D et 3D de manière efficace. Pour le rapport, nous avons utilisé overleaf, qui nous a permis de 
collaborer sur la rédaction du document en temps réel. Pour la documentation, nous avons utilisé Doxygen, qui nous a permis de générer automatiquement la 
documentation du code à l'aide de nos commentaires. Enfin, pour la communication, nous avons utilisé instagram, qui nous a permis de discuter en temps réel 
et de partager des idées rapidement.
\section{Développement}



\subsection{Logique d'execution du  jeu et fonctionnement du menu}
\indent Dans cette section on explique la logique de fonctionnement du fichier main.c, et par la même ocasion, la naviguation du joueur dans le menu du jeu.
Ce menu permet d'afficher différents boutons utilent à la navigation du joueur CF photo ANNEXE ... .
\\
\\
Pour comprendre comment fonctionnent la logique du jeu dans main.c, on a besoin d'une variable importante : CurrentScreen, c'est un pointeur sur un Gamescreen, qui decris l'etat de jeu actuel. 
Cet variable peut prendre diverse valeurs comme NOUVELLE PARTIE, CHARGER PARTIE ou encore MENU. Par default currentScreen vaut MENU. 
Il faut maintenant comprendre comment le jeu tourne. Pour cela, dans main.c, on utilise une grande boucle while tournant tant qu'une variable running n'est pas mise à FALSE (ou que le joueur ne supprime pas sa fenetre).
Dans cette boucle il faut distinguer la partie logique du jeu de la partie dessin, chaque partie correspond à un switch case. Chaque switch case regarde que vaut la variable CurrentScreen. Dans la partie logique on mets à jours les variables globales, on appel des fonctions autonomes qui initialisent ou mettent à jours nos variables globales en fonction de différents test.
Un bonne exemple de fonction destiné à la partie logique est UpdateGame(...) (HREF SUR ARGU ET COMM EN ANNEXE)  qui mets à jours toute la logique du jeu en mode solo. 
Une fois la logique à jours un peut dessiner. Le second switch entre en jeu et permet de lancer les bonne fonction de dessin toujours en fonction de la valeurs de CurrentScreen
Desormais, pour le menu, il suffit de voir que lorsque CurrentScreen vaut MENU on appel les fonctions gerant la souris, le clavier et on dessinent les boutons voulus.





\subsection{Création de la carte}

La carte du jeu est un labyrinthe 3D généré de manière procédurale à partir d'une grille
de \texttt{NUM\_BLOCKS} $\times$ \texttt{NUM\_BLOCKS} blocs. Chaque bloc possède une
position dans l'espace, des dimensions fixes $(3 \times 6 \times 3)$ et un état binaire :
\textbf{mur} (\texttt{isWall = 1}) ou \textbf{passage} (\texttt{isWall = 0}).

La génération commence par l'initialisation de tous les blocs en murs, disposés en grille
avec un espacement de 3 unités. L'algorithme utilisé pour creuser les passages est un
parcours en profondeur d'abord (DFS) itératif, implémenté à l'aide d'une pile maison
(\texttt{pile.h}) afin d'éviter tout débordement de pile récursive. En partant de la
case $(1, 1)$, l'algorithme recherche à chaque étape les voisins situés à une
\textbf{distance de 2 cases} qui sont encore des murs. Ce saut de deux cases est
essentiel : il garantit qu'un bloc mur sépare toujours deux cellules adjacentes, ce qui
est la condition nécessaire pour obtenir un labyrinthe valide. Un voisin est alors
choisi aléatoirement, la cellule cible est ouverte, ainsi que le bloc intermédiaire qui
la sépare de la case courante. Si aucun voisin valide n'est disponible, l'algorithme
\textbf{dépile} (\textit{backtrack}) pour revenir à une case précédente disposant encore
de voisins non visités. Ce processus se répète jusqu'à ce que la pile soit vide, garantissant
que chaque cellule de la grille est visitée exactement une fois et que le labyrinthe
généré est parfait, c'est-à-dire sans cycle et avec un chemin unique entre deux cases.

En mode multijoueur, une variante \texttt{creer\_lab\_multi} est utilisée. Elle génère
d'abord un labyrinthe classique, puis modifie le tiers central de la grille pour créer
une grande arène ouverte. Dans cette zone, 90\% des blocs sont vidés pour laisser un
espace dégagé, tandis que les 10\% restants sont conservés comme obstacles aléatoires
afin d'animer l'espace et d'offrir des possibilités tactiques aux joueurs.

L'affichage de la carte est assuré par \texttt{DrawLevel}, qui parcourt l'intégralité de
la grille à chaque frame et dessine chaque bloc selon son état. Les blocs murs sont
rendus avec un modèle 3D haut (\textit{scale} $3 \times 6 \times 3$), tandis que les
passages sont affichés comme une dalle de sol ultra-plate (\textit{scale}
$3 \times 0{,}001 \times 3$). Pour éviter les artefacts visuels, les blocs ouverts lors
de la génération ont leur coordonnée $y$ fixée à $-20$, les dissimulant ainsi sous le sol,
seule la dalle étant rendue à $y = 0$.




\subsection{Joueur, déplacement et gestion de la vie}

Le joueur est représenté par une structure \texttt{Entity} initialisée par \texttt{InitPlayer},
qui définit l'ensemble de ses attributs de base : une position de départ en hauteur
(\texttt{y = 10}), 100 points de vie sur un maximum de 100, 3 vies, une taille de 1 unité
utilisée pour les calculs de collision, ainsi qu'une arme de départ (le pistolet) avec ses
munitions chargées au maximum. Un chronomètre de tir \texttt{chronoTir} est également initialisé
à 0 pour permettre de tirer immédiatement dès le début de la partie. Par défaut, aucune arme
supplémentaire n'est débloquée (\texttt{armeUnlock} initialisé à $-1$).

Le déplacement et la mise à jour de l'état du joueur sont entièrement gérés à chaque frame par
\texttt{UpdatePlayer}. L'orientation de la caméra est contrôlée à la souris via deux angles :
le \texttt{yaw}, qui correspond à la rotation horizontale gauche/droite, et le \texttt{pitch},
qui correspond à l'inclinaison verticale haut/bas. Ce dernier est borné à $\pm 1{,}5$ radians
(environ $\pm 85°$) afin d'éviter un retournement complet de la caméra. À partir du \texttt{yaw},
un vecteur \textbf{forward} est calculé \textit{uniquement} sur le plan horizontal $(XZ)$, ce qui
garantit que le joueur se déplace toujours à plat, sans s'envoler, même lorsqu'il regarde vers
le ciel ou vers le sol.

Les touches directionnelles (\texttt{W/A/S/D} ou les flèches) construisent un vecteur de
mouvement en combinant le vecteur \textbf{forward} et son vecteur orthogonal pour les
déplacements latéraux. Ce vecteur est ensuite \textbf{normalisé} avant d'être multiplié par la
vitesse, ce qui empêche le joueur de se déplacer plus vite en diagonale qu'en ligne droite —
sans cette normalisation, la longueur du vecteur diagonal serait $\sqrt{2} \approx 1{,}41$ fois
supérieure. Les sons de pas sont également déclenchés ou mis en pause selon que le joueur est
au sol ou en l'air. La gravité est simulée en décrémentant \texttt{velocityY} d'une valeur fixe
à chaque frame, ce qui produit une accélération progressive vers le bas. Le joueur peut sauter
en appuyant sur \texttt{ESPACE} uniquement lorsqu'il est détecté comme étant au sol
(\texttt{onGround = true}), ce qui empêche les sauts successifs en l'air.

La gestion des collisions consiste à calculer une position future \texttt{nextPos} avant de
l'appliquer, puis à vérifier si cette position entre en conflit avec les blocs de la scène ou
les ennemis. En cas de collision, trois situations sont distinguées : une chute sur le dessus
d'un bloc, qui pose le joueur à sa surface et réinitialise \texttt{velocityY} à 0 en activant
\texttt{onGround} ; un choc par le dessous lors d'un saut, qui stoppe immédiatement la montée ;
ou une collision latérale contre un mur. Dans ce dernier cas, l'axe de \textbf{moindre
pénétration} est identifié en comparant les chevauchements sur $X$ et $Z$, et le joueur est
repoussé uniquement sur cet axe en laissant l'autre intact, produisant un effet de
\textbf{glissement fluide} le long des murs. Ce même système est appliqué aux interactions
avec les ennemis, avec en plus la possibilité d'atterrir sur la tête d'un adversaire et d'y
effectuer un nouveau saut. Enfin, la caméra Raylib est recalculée à chaque frame en positionnant
son origine légèrement au-dessus du centre du joueur (\texttt{+0.5f} sur $Y$ pour simuler le
niveau des yeux), et son point cible est obtenu en additionnant la position et le vecteur
directionnel 3D complet, calculé en coordonnées sphériques à partir du \texttt{yaw} et du
\texttt{pitch}.

Le joueur a également la possibilité de soigner, en passant sur des caisses dissiminés partout dans le labyrinthe.
Elles sont au nombres de 10 et apparaissent aléatoirement. Quand on passe dessus, il y a 50\% de chances de se soigner
de 20 points de vie, 35\% pour 40, 14\% pour 60 et 1\% pour 100. Le joueur peut aussi récupérer des armes
dans les caisse. IL a ainsi 70\% de chance de ne rien récupérer, 18 \% pour un sniper,
9\% pour le fusil d'assault et 1\% pour la grenade. Une fois une caisse récupéré, elles disparaissent
pour se remettre à autre endroit aléatoire du labyrinthe. Cette mécanique de caisse de soin et d'armes
est developpé en mode solo mais également en mode multijoueur.




\subsection{Bots, leurs IA, spawn, boss}
Concernant les bots, ils sont guidés par une intelligence artificielle simple mais efficace. Ils empruntent le chemin le plus court vers le joueur dans le 
labyrinthe, mais dès qu'ils détectent le joueur à une certaine distance, ils commencent à le poursuivre en ligne droite ce qui permet d'éviter des calculs inutiles. 
Sinon si le joueur n'est pas visible ils utilisent un algorithme de recherche de chemin pour trouver le chemin le plus court vers le joueur, en évitant les obstacles 
du labyrinthe. Les bots ont également une capacité d'attaque, et lorsqu'ils sont suffisamment proches du joueur, ils peuvent lui infliger des dégâts. Les boss, quant 
à eux, sont des ennemis plus puissants avec des comportements plus complexes. Ils utilisent un sniper plutot que le glock des bots normaux, ce qui fait qu'ils infligent 
un nombre de dégats beaucoup plus élevé, leur IA est beaucoup plus précise (ils ne ratent pas une cible à moins qu'elle ne soit cachée derrière un mur ou qu'elle bouge 
très rapidement) et ils ont plus de points de vie que les bots normaux. Ils apparaissent à des moments clés du jeu pour offrir un défi supplémentaire au joueur. 
Néanmoins, ils se déplacent 2 fois moins vite que les bots normaux ce qui fait qu'ils sont plus facile à fuir que les bots normaux, mais ils sont plus dangereux s'ils 
arrivent à vous toucher. les bots et les boss ont la même logique de spawn, ils apparaissent à des endroits aléatoires du labyrinthe, bien que les boss apparaissent 
tout les 50 kills de bots classiques. 

L'algorithme de recherche de chemin utilise 3 matrices. La première se nomme \emph{visited} et contient des booléens indiquant si une case à dejà été visitée ou pas, 
la suivant s'appelle \emph{parent} et contient quelle case a été visitée avant d'arriver à la case actuelle, et enfin la troisième matrice \emph{queue} contient la 
liste des cases à visiter. Ces trois matrices permettent d'éffectuer une recherche en largeur pour trouver le chemin le plus court vers le joueur. Donc comme si on 
versait de l'eau dans le labyrinthe à partir de la position du bot, et que l'eau se propageait dans toutes les directions en évitant les murs, la troisième matrice 
contiendrait la liste des cases où l'eau est présente à chaque instant, la première matrice indiquerait quelles cases ont déjà été touchées par l'eau, et la deuxième 
matrice indiquerait pour chaque case quelle est la case précédente d'où l'eau est venue. Lorsque l'eau atteint la position du joueur, on peut remonter le chemin en 
utilisant la deuxième matrice pour trouver le chemin le plus court vers le joueur.

Les bots et les boss sont du même type abstrait, ils sont tous les deux des \emph{Entity}. Les {Entity} sont des tous les objets animés soit par un être humain comme 
par exemple les \emph{ENTITY\_PLAYER} qui ont été vu dans la partie sur les joueurs ou par une intelligence artificielle comme les \emph{ENTITY\_BOT} et les 
\emph{ENTITY\_BOSS}. Dans notre cas on va plutot s'intéresser à ceux gérer par des IA. Les bots et les boss ont des caractéristiques communes, comme une position 
générer aléatoirement parmi les cases du labyrinthe qui ne sont pas des murs, une direction géré à de l'algorithme de recherche dans le labyrinthe. Ils ont également 
des comportements communs, comme se déplacer vers le joueur, attaquer le joueur lorsqu'ils sont suffisamment proches, et réagir aux dégâts qu'ils subissent. Cependant, 
les boss ont des caractéristiques plus puissantes que les bots normaux, comme plus de points de vie, une arme plus puissante, et une IA plus précise.

Les bots possèdent également une mécanique de salto. Il s'agit d'une mécanique qui est effectuée de manière aléatoire mais qui lui permet d'esquiver certains tirs 
adverses. Lorsque le bot décide de faire un salto, il commence à tourner sur lui même à une vitesse de 720 degrés par seconde pendant une durée de 0.5 secondes, ce 
qui lui peut permet d'esquiver les tirs ennemis pendant cette période. Cependant, le bot ne peut pas faire de salto indéfiniment, il doit attendre un certain temps 
avant de pouvoir en faire un autre. Le salto est une mécanique qui rend les bots plus difficiles à toucher et ajoute du dynamisme aux combats, mais elle est équilibrée 
par le fait que les bots ne peuvent pas en faire trop souvent.

En cas de mort d'un bot il réapparait immédiatement à un autre endroit du labyrinthe, ce qui permet de maintenir une pression constante sur le joueur et d'assurer que 
le jeu reste dynamique. Cependant, les boss ne réapparaissent pas immédiatement après leur mort, ils réapparaissent si le score du joueur atteint un certain seuil, ce 
qui ajoute une dimension de progression au jeu et encourage le joueur à éliminer les bots pour faire réapparaître les boss.
\subsection{Armes, munitions, dégâts}
\subsection{Multijoueur, réseaux}
Dans le mode multijoueur, les joueurs s'affrontent dans un labyrinthe revisité pour faciliter les combats longue distance. Les joueurs peuvent se déplacer librement dans 
le labyrinthe et utiliser différentes armes pour éliminer leurs adversaires. Les armes disponibles ont été vu dans la section précédente sur les armes. L'objectif des 
joueurs dans ce mode est d'éliminer 25 fois son adversaire pour gagner la partie. Les joueurs peuvent également utiliser des power-ups qui apparaissent aléatoirement 
dans le labyrinthe pour obtenir des avantages temporaires, comme les caisses de heal réparties aléatoirement sur la map. 

Le mode multijoueur utilise une architecture client-serveur, où un joueur agit en tant que serveur et l'autre joueur se connecte en tant que client. Le serveur est 
responsable de la gestion de l'état du jeu, de la synchronisation des actions des joueurs, et de la communication avec le client. Le client envoie ses actions au serveur, 
qui les traite et met à jour l'état du jeu en conséquence. La communication entre le serveur et les clients se fait via des sockets, ce qui permet une transmission 
efficace des données en temps réel.

Le réseau est géré de manière à assurer une synchronisation efficace entre les joueurs. Le serveur envoie régulièrement des mises à jour de l'état du jeu au client. La 
gestion du transfert des données, s'effectue via le protocole TCP dans la majorité des cas, qui garantit que les données sont transmises de manière fiable et dans le bon ordre. 
Le serveur écoute les connexions entrantes et gère les sessions de jeu pour le client. Ce protocole est couplé à une fonction qui permet de rendre les sockets non bloquants. 
Ce qui est d'une importance cruciale pour maintenir une expérience multijoueur fluide et réactive. En utilisant des sockets non bloquants, le serveur peut continuer à traiter 
les actions du jeu et à envoyer des mises à jour au client sans être interrompu par des opérations de réseau, ce qui améliore considérablement la réactivité du jeu et 
l'expérience globale pour les joueurs.

Les joueurs ont deux méthodes pour se connecter à un serveur. La première méthode consiste à entrer l'adresse IP du serveur dans le menu de connexion du jeu, ce qui permet au 
client de se connecter directement au serveur néanmoins cette méthode requiert que le serveur ait au préalable ouvert le port 30000 et ait fait en sorte que lorsque l'on entre 
son adresse IP publique la demande de connexion a ce port redirige directement la requête vers sa machine. Dans ce cas c'est le protocole TCP qui est utilisé. La deuxième méthode 
consiste à jouer la partie en local, c'est à dire que les deux joueurs soient connectés à un même réseau. Dans ce cas le client peut se connecter automatiquement grâce à un 
broadcast local. Cette méthode est plus simple à mettre en place pour les joueurs, mais elle nécessite que les deux joueurs soient sur le même réseau, ce qui peut limiter les 
possibilités de jeu en ligne mais est néanmoins plus sure pour le joueur qui héberge le serveur, car il n'a pas besoin d'ouvrir de port ou de gérer les redirections de port, 
ce qui peut être complexe et potentiellement risqué en termes de sécurité pour sa machine. Dans le cas du broadcast local, le protocole utilisé est UDP, qui permet une découverte 
rapide des serveurs disponibles sur le réseau local sans nécessiter de configuration préalable.

Pour la gestion du multijoueur, chacun gère ses données c'est à dire que si le client se fait tuer, il envoie un message au serveur pour lui indiquer qu'il a été tué, et le serveur 
met à jour son score. De même, lorsque le serveur est vaincu par son adversaire, il envoie un message au client pour lui indiquer qu'il a été tué, et le client met à jour son score. 
Cette approche est certes plus simple à mettre en place, mais elle peut être sujette à des problèmes de triches, car un joueur malveillant pourrait envoyer de faux messages pour 
manipuler le score. Cependant, pour un projet de cette envergure, cette méthode est suffisante pour assurer une expérience multijoueur fonctionnelle et agréable, tout en minimisant 
la complexité du développement.
\section{Résultats et conclusion}
\subsection{Bilan temporel}
\subsection{Bilan de la conformité aux cahiers des charges}
\subsection{Améliorations possibles}
\section{Annexes}


\end{document}